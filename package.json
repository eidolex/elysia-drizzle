{
  "name": "elysia-app",
  "version": "1.0.50",
  "scripts": {
    "db:generate": "bun drizzle-kit generate:mysql",
    "db:migrate": "bun run src/database/migrate.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.2",
    "elysia": "latest",
    "mysql2": "^3.9.2"
  },
  "devDependencies": {
    "bun-types": "latest",
    "drizzle-kit": "^0.20.14"
  },
  "module": "src/index.js"
}
